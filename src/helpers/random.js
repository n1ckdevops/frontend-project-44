// random num func
export const randomNum = (min, max) => {
  return Math.floor(Math.random() * (min + max) + min);
};

// random operator (we'll get straight to this a bit later, boys)
// lets dive into even func itself (to find out whether the number is even or not)
//
